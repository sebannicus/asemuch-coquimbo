"use client";
import { useActionState } from "react";
import { SubmitButton } from "./SubmitButton";
const initial: { error: string | null } = { error: null };
export function DocumentForm({ action, item }: { action: (s: typeof initial, f: FormData) => Promise<typeof initial>; item?: Record<string, string | null> }) { const [state, formAction] = useActionState(action, initial); return <form action={formAction} encType="multipart/form-data" className="space-y-4 rounded-3xl border border-[#d9e6f5] bg-white p-6">{item?.id ? <input type="hidden" name="id" value={item.id} /> : null}<h2 className="text-2xl font-extrabold text-[#0c2340]">{item ? "Editar documento" : "Nuevo documento"}</h2><input name="title" required minLength={3} defaultValue={item?.title ?? ""} placeholder="Título" className="w-full rounded-xl border p-3" /><input name="slug" defaultValue={item?.slug ?? ""} placeholder="Enlace opcional" className="w-full rounded-xl border p-3" /><input name="document_type" required defaultValue={item?.document_type ?? ""} placeholder="Tipo: acta, circular, reglamento..." className="w-full rounded-xl border p-3" /><textarea name="summary" required minLength={10} defaultValue={item?.summary ?? ""} placeholder="Resumen" className="w-full rounded-xl border p-3" rows={4} /><input name="file" type="file" accept="application/pdf" className="w-full" /><select name="status" defaultValue={item?.status ?? "draft"} className="rounded-xl border p-3"><option value="draft">Borrador</option><option value="published">Publicado</option></select>{state.error ? <p className="text-rose-700">{state.error}</p> : null}<SubmitButton /></form>; }
